language: php

php: 
  - 5.5
  
  
build:
 cache: true
 cache_dir_list:
   - $HOME/.phpenv/versions/$(phpenv version-name)/lib/php/extensions/no-debug-non-zts-20131226


 ci:
  - echo $(phpenv version-name) 
  - pickle install intl
  - cat $HOME/.phpenv/versions/$(phpenv version-name)/lib/php/extensions/
  - mkdir -p shippable/codecoverage
  - mkdir -p shippable/testresults
  - phpunit  --log-junit shippable/testresults/junit.xml --coverage-xml shippable/codecoverage tests/calculator_test.php
